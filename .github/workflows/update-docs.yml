name: Update Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/**'

jobs:
  update-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.11'

      - name: Regenerate documentation
        run: |
          pip install skillsaw==0.14.0
          skillsaw docs -o docs/ --theme crimson-red

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "docs: auto-regenerate plugin documentation"
          title: "docs: auto-regenerate plugin documentation"
          body: |
            Automatically regenerated plugin documentation from skillsaw.
          branch: auto-update-docs
          delete-branch: true
          add-paths: |
            docs/
